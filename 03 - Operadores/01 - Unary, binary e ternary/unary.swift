// Operador Unary

/*

    É um operador que atua sobre apenas um operando (um valor ou variável) para executar a sua função.

    Exemplos:

    ★ -a → negação (inverte o sinal);
    ★ ++a ou a++ → incremento;
    ★ --a ou a-- → decremento;
    ★ !a → negação lógica (not).
*/

// Ligar lâmpada
var lampada: Bool = false
print (!lampada) // Ou usar "lampada.toggle()" → Vai realizar a inversão e atribuir o valor à variável